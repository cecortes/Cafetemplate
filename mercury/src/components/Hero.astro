---
interface Props {
  img: string;
}

const { img } = Astro.props;
---

<section>

  <!-- Background Image -->
  <div class="hero" style={{ backgroundImage: `url(${img})` }} aria-hidden="true"></div>

  <div class="main-container">

    <div class="titles-container">
      <h1 class=" text-slate-800 text-shadow-stone-100 text-4xl md:text-6xl">Bienvenido a Rentoy</h1>
      <h2 class="subtitle">Un rincón para disfrutar del mejor café y comida casera en Puebla.</h2>
    </div>
  
    <div class="content-container flex flex-column md:flex-row md:justify-between md:mt-8 md:w-[80%] md:gap-4">
      <article class="glass mb-6 md:w-[40%]">
        <p class="text-slate-800 text-shadow-stone text-justify text-pretty  lg:text-2xl">
          Ubicado en el corazón del Centro Histórico de Puebla, Rentoy Café Restaurante es mucho más que un lugar para tomar café: es un refugio cultural y gastronómico que invita a detener el tiempo. Justo al lado del majestuoso Teatro Principal y enmarcado por el encanto bohemio del Barrio del Artista, Rentoy se convierte en una parada obligada para locales y visitantes que buscan una experiencia única.
        </p>
      </article>

      <div class="container hidden md:w-[40%] mb:block"></div>
    </div>

  </div>

</section>

<style>
  .hero {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    z-index: -1;
    pointer-events: none;
  }
  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(58, 58, 58, 0.1);;
  }
  .main-container {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    height: 100vh;
  }

  .titles-container {
    margin: 0 auto;
    padding: 0 1rem;
    max-width: 800px;
    margin-bottom: 2rem;
  }

  .glass {
    /* From https://css.glass */
    background: rgba(255, 255, 255, 0.2);
    border-radius: 16px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(5px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    padding: 1.5rem;
  }

</style>